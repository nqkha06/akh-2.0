# Server-only URL used by Server Components and route handlers.
API_INTERNAL_URL="http://localhost:4000/api"

# Public Google Identity Services client ID. The credential is verified by NestJS.
NEXT_PUBLIC_GOOGLE_CLIENT_ID="491488922287-06h8qfmt42lebaca62strlu7pgrgqmrf.apps.googleusercontent.com"

AUTH_ACCESS_COOKIE_NAME="stu_access_token"
AUTH_REFRESH_COOKIE_NAME="stu_refresh_token"

# Used only when the proxy does not provide a country header (for example local dev).
VISITOR_COUNTRY_FALLBACK="VN"
